{
  "name": "webos-clipfm",
  "version": "1.1.0",
  "scripts": {
    "build": "node ./scripts/build.js",
    "package": "ares-package -o dist src",
    "apply": "ares-install --device emulator ./dist/com.domain.clipfm_1.1.0_all.ipk",
    "launch": "ares-launch --device emulator com.domain.clipfm"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
